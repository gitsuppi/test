import { unstable_deprecatedPropType as deprecatedPropType } from '@mui/utils';

expimport { unstable_deprecatedPropType as deprecatedPropType } from '@mui/utils';

export default deprecatedPropType;
